## v1.8.38 (2026-07-08)

### 功能
- 首页分类和追番按最近观看时间排序
- PotPlayer 风格缩略图预览（预抽+缓存文件+asset协议加载）
- FFmpeg 进程管理：切换视频时自动 abort 旧任务

### 修复
- 禁用播放器窗口 IME 注入（防止微信输入法崩溃）
- 缩略图色彩格式修正（yuvj420p）
- asset URL 带版本号防缓存失效

## v1.8.37 (2026-07-07)

### 修复
- 方案1: mpv 降级为纯软件渲染 (--vo=gpu, --hwdec=no, --vd-lavc-threads=1)
- 方案2: 前端 cleanup 先发 quit 再等 500ms 再 destroy，避免 GPU 资源清理冲突
- 方案3: Rust 侧窗口销毁延迟 600ms，先 hide 再 destroy

## v1.8.36 (2026-07-07)

### 修复
- 小窗操作按钮改用 position:fixed 定位，避免父容器布局影响导致图标缩放异常
- 按钮尺寸 30x30，SVG 图标 20x20

## v1.8.35 (2026-07-07)

### 功能
- 首页分类按最近观看时间排序
- 小窗播放按钮改为 hover 显示/隐藏
- 小窗三个按钮统一为 SVG 图标（Solar pin + PiP exit + 关闭），同系列无多余装饰

## v1.8.34 (2026-07-07)

### 变更
- 清理死代码：移除 windowDraggingRef 和 aspectResizeLockRef 及相关逻辑
- 小窗置顶图标改回 Solar pin linear，SVG 尺寸放大至 26px

## v1.8.33 (2026-07-07)

### 变更
- 小窗置顶 SVG 图标放大至 22px，填满按钮

## v1.8.32 (2026-07-07)

### 修复
- 移除 on_window_event(Destroyed) 中的 destroy() 调用，IPC 通道在窗口销毁时已断开，强行调用导致 ntdll 堆损坏
- mpv 清理由前端 useEffect cleanup 的 destroy() 负责，确保在 IPC 仍可用时执行

## v1.8.31 (2026-07-07)

### 核心修复
- mpv 改为 --wid 嵌入模式，消除双窗口 Vulkan Swapchain 竞争导致的 ntdll 崩溃
- Rust 新增 get_player_hwnd 命令，前端获取 HWND 后传给 mpv
- 删除 --force-window=yes（覆盖 --wid 导致 mpv 自己建窗的根因）
- 删除 onResized 中的 settle timer setSize 逻辑（mpv 作为 child window 自行处理 WM_SIZE）

### 变更
- 小窗置顶按钮 SVG 放大至 18px，与关闭按钮大小一致

## v1.8.30 (2026-07-07)

### 修复
- 缩略图预览从 video+canvas 方案替换为 Rust 侧 FFmpeg 抽帧，彻底解决 Windows 上黑帧/空白帧问题
- 预览加载时间 <500ms，支持所有本地视频格式（MP4/MKV/HEVC/4K）
- LRU 缓存 1000 条（2秒粒度），相同时间点不重复调用 FFmpeg
- 旧 video/canvas 代码已完全移除，不再与主 mpv 进程抢占 GPU 资源

## v1.8.29 (2026-07-07)

### 变更
- 置顶按钮图标换为 Solar pin linear SVG
- 标题栏置顶按钮移到最小化左侧
- 小窗置顶按钮移到退出小窗左侧，改为圆形图标按钮

## v1.8.28 (2026-07-07)

### 调试
- mpv 启动参数加 --log-file=mpv.log，崩溃后可查 mpv 内部日志
- 暂时移除 time-pos 属性监听，排查堆损坏来源

### 修复
- Rust 端窗口关闭/销毁时主动调用 mpv.destroy() 确保 mpv 进程被终结
- mpv 参数恢复 d3d11va + d3d11（与用户 GPU 驱动兼容）

## v1.8.27 (2026-07-07)

### 新功能
- 播放器标题栏新增置顶按钮（图钉图标），位于最小化左边
- 小窗模式下新增置顶按钮，位于退出小窗左边
- 置顶时图标变为粉色高亮

### 修复
- 窗口拉伸期间完全屏蔽 onResized IPC，减少堆损坏崩溃风险
- 全屏时跳过 onResized IPC，防止全屏后崩溃

## v1.8.26 (2026-07-07)

### 修复
- 全屏时跳过 onResized IPC 调用，防止全屏后几秒崩溃

## v1.8.25 (2026-07-07)

### 修复
- 窗口拉伸时跳过 onResized IPC 调用，减少堆损坏崩溃风险

## v1.8.24 (2026-07-07)

### 修复
- 播放器选集侧边栏鼠标快速移过时能正确关闭

## v1.8.23 (2026-07-07)

### 性能优化
- 所有页面操作改为乐观更新：追番、已看完、连载状态、中文字幕、删除、切换分类、批量操作等，UI 立即响应不卡顿

### 修复
- 编辑更换海报时预览图片缓存问题，新海报立即显示

## v1.8.22 (2026-07-07)

### 修复
- 选集卡片始终使用横版比例，不再受视频集海报方向影响

## v1.8.21 (2026-07-07)

### 修复
- 修复 Windows 路径比较：same_dir 统一处理反斜杠，季文件夹海报正确识别

## v1.8.20 (2026-07-07)

### 修复
- 修复 mpv 路径解析：尝试多种路径格式定位 bundled mpv.exe

## v1.8.19 (2026-07-07)

### 修复
- 播放器启动失败或加载视频失败时自动关闭窗口（2秒后），不再残留空白窗口

## v1.8.18 (2026-07-07)

### 修复
- 播放器启动失败时显示具体错误信息（而非通用提示）
- 启用 mpv 输出日志（showMpvOutput）辅助调试

## v1.8.17 (2026-07-07)

### 修复
- 标签功能开启的分类下，非标签子文件夹的视频集也能被检查更新识别到
- 删除视频集时同步删除关联的分集数据（之前只删了视频集记录）
- 多季视频集海报优先取季文件夹海报，回退到根目录海报
- 删除选集右键菜单，改为选择模式批量删除

## v1.8.16 (2026-07-07)

### 新增
- 搜索结果页面视频集支持右键菜单（编辑、追番、已看完、检查更新、切换分类、删除）

## v1.8.15 (2026-07-07)

### 修复
- 检查更新添加视频时自动推断季号和分配集数
- 分类文件夹下直接放视频集也能识别到
- 重新添加视频集时自动更新到当前分类

## v1.8.14 (2026-07-07)

### 修复
- 从视频集详情页返回库页面时恢复之前的滚动位置

## v1.8.13 (2026-07-07)

### 修复
- 返回按钮改为直接导航，不再依赖浏览器历史
- 编辑分类弹窗布局调整：扫描路径位置、卡片方向和集数称呼同行
- 功能开关标签样式统一
- 追番标记提示去掉"收藏"
- mpv 路径从打包资源解析，用户无感安装
- 分类功能标签改为彩色配色

## v1.8.13 (2026-07-07)

### 修复
- 返回按钮改为直接导航，不再依赖浏览器历史
- 编辑分类弹窗：扫描路径移到分类名称下方，卡片方向和集数称呼同行显示
- 功能开关标签样式统一
- 追番标记提示文案去掉"收藏"
- mpv 路径从打包资源解析，用户无感安装

## v1.8.12 (2026-07-07)

### 修复
- 替换播放器插件：tauri-plugin-libmpv → tauri-plugin-mpv（JSON IPC，消除堆损坏闪退）
- 文件名末尾数字集数识别（如 xxx 01）

## v1.8.11 (2026-07-07)

### 修复
- 关闭播放器堆损坏闪退加强：quit 命令 + 800ms 等待 + 1500ms 兜底杀进程
- 键盘方向键快进快退改为 10 秒
- 底部控制栏 position fixed 贴窗口底部
- 右键菜单追番/已看完后台静默执行，成功失败都有通知
- 番号 -C 中文字幕检测不限 display_type
- 追番标记和观影进度拆分为独立功能开关
- 分集标题改为「选集」，空状态改为「暂无资源」
- 暂无资源的视频集不显示观看状态文字
- 删除死代码 VideoDetail 页面（-732 行）

## v1.8.11 (2026-07-07)

### 修复
- 键盘左右方向键快进快退改为 10 秒
- 底部控制栏贴着窗口底部，不再超出或被裁断
- 右键菜单追番/已看完操作改为后台静默执行，成功或失败都有通知提醒
- 番号 -C 中文字幕检测不再限制 display_type，任何分类都识别
- 追番标记和观影进度拆分为独立功能开关

## v1.8.10 (2026-07-07)

### 新增
- 选择模式下支持批量切换分类

### 优化
- 分类编辑弹窗：集数称呼从功能区移到卡片方向下方，两个选择框并排
- 分类卡片展示：集数称呼与卡片方向同行显示（如"卡片方向: 自动 · 集数称呼: 话"），不再作为功能标签

## v1.8.9 (2026-07-07)

### 新增
- 选择模式下支持批量切换分类：选中视频集后点击「切换分类」按钮，选择目标分类即可批量切换

## v1.8.8 (2026-07-07)

### 修复
- 修复关闭播放器/2倍速播放时堆损坏闪退：前端销毁 mpv 前先暂停+停止事件+等待回调排空，Rust 端延迟 800ms 兜底杀进程
- 播放器内切换集数黑屏有声：切集后等待 mpv 出帧，同分辨率视频兜底确保黑底消失
- 选集侧边栏上下都不空：position: fixed 填满整个窗口
- 视频区域同时支持拖拽移动窗口和单击播放/暂停
- 分类弹窗卡片方向下拉框改为 BubbleSelect 样式
- 功能开关顺序调整为：追番/完结、连载状态、标签、演员、中文字幕、剧集单位
- 分类功能标签 status 显示为"连载状态"

## v1.8.5 (2026-07-06)

### 修复
- 播放器拖拽/拉伸/倍速操作时闪退：onResized 回调加 100ms debounce，合并 IPC 调用防止 Tauri IPC 层过载崩溃
- 播放器底部导航栏被裁断：overflow 改为 visible，加回 JS 高度同步
- 播放器选集侧边栏上下留空：控制栏恢复 absolute 定位，不占 flex 空间
- 隐藏右下角拉伸手柄的竖线指示器
- 点击视频区域无法播放/暂停：排除列表加 .changli-player-stage，不再触发窗口拖拽
- 视频集排序修复：标签/演员筛选查询从 updated_at 改为 created_at 排序
- 已看完/连载状态切换后同步刷新：store 更新时清除标签/演员筛选缓存
- 右键菜单文案：追番→添加到追番/取消该追番，切换类型→切换分类
- 简介位置：非编辑状态下移到标签/演员下方

## v1.8.4 (2026-07-06)

### 修复
- 修复播放器底部导航栏定位问题：用 Tauri outerSize() 获取真实窗口尺寸同步视口高度，修复 html/body/#root 高度链断裂，修复 CSS `> *` 规则强制覆盖底部栏定位
- 播放器布局改为 CSS flexbox，控制栏作为 flex item 自然贴底，不依赖 absolute/fixed 定位
- 右下角拉伸锁定为自定义 JS 实现，Rust 端 resizable(false) 禁止系统自由拉伸，全程按视频比例计算宽高

## v1.8.3 (2026-07-06)

### 修复
- 修复播放器底部导航栏在透明 WebView 窗口下错位跑到上面的问题：窗口缩放时实时同步 HTML 高度，底部栏改回 absolute 定位贴合真实窗口底部
- 右下角拉伸改为系统原生 `startResizeDragging('SouthEast')`，拖动结束后再做等比校正，缩小时不再疯狂闪烁
- 视频承载区中间保持透明，视频未出帧前短暂黑底兜底，出帧后不遮挡画面

## v1.8.2 (2026-07-06)

### 修复
- 修复播放器底部导航栏在窗口缩放后可能上移的问题，控制栏会固定贴合窗口底部
- 优化播放器右下角按比例缩小窗口的流畅度，减少缩小时的闪烁和卡顿

## v1.8.1 (2026-07-06)

### 修复
- 修复播放器视频承载区在透明显示和黑白遮挡之间反复冲突的问题：播放画面准备前短暂黑底兜底，画面就绪后中间区域保持透明交给 libmpv 显示
- 播放器窗口禁用系统四边自由拉伸，改为仅右下角按当前视频比例联动拉伸，减少黑边和闪屏

## v1.8.0 (2026-07-06)

### 修复
- 修复播放器倍速选择框向上展开后，鼠标移入选项区域会导致底部导航栏收起、无法正常选择倍速的问题
- 播放器右下角调整窗口大小时不再误唤起右侧选集栏或底部导航栏
- 播放器手动调整窗口大小时会按当前视频比例联动宽高，减少黑边填充
- 小窗播放进入和手动调整大小时同样按当前视频比例匹配窗口，避免小窗模式继续出现多余黑边
- 小窗播放底部新增进度条，并增加 1X/2X 倍速切换按钮
- 播放器进度条支持按住拖拽调整播放位置，拖动时进度和时间同步跟随
- 修复播放器长时间播放时 IPC 健康检查误判异常，导致提示重新打开并遮住画面的问题
- 修复播放器透明兜底层过度放开导致的窗口透明问题，保持视频承载层黑底兜底
- 视频集右键菜单新增连载状态切换，可在“连载中”和“已完结”之间快速切换
- 连载状态变更后同步刷新库列表缩略图文案，“更新至第 x话”和“全 x话”会跟随状态切换
- 连载状态、中文字幕等双态开关加入滑块移动动画，切换更顺滑
- 视频集编辑态恢复标题、简介字段标签，并优化空内容占位文案

## v1.7.98 (2026-07-06)

### 优化
- 全量检查更新执行阶段改为后台全局任务，切换页面后仍会继续完成
- 优化全量检查更新的执行速度：新发现分集、视频集添加和本地已删除资源处理改为批量执行
- 全量检查更新判断新发现视频集前会自动排除已存在于其他分类的视频集，避免跨分类转移后重复提示
- 全量检查更新按钮状态区分“检查中”和“更新中”，执行阶段反馈更准确

### 修复
- 修复播放器可能出现白屏有声音的问题，视频画面不再被播放窗口背景遮挡
- 暂无海报时显示的品牌 logo 改为圆角样式，和应用图标视觉保持一致

## v1.7.97 (2026-07-06)

### 优化
- 批量更新海报未开始前不再展示进度状态，执行后再显示“海报更新进度”
- 单个视频集检查更新支持勾选新发现分集和本地已删除分集，取消勾选的内容不会变更
- 单个视频集检查更新结果通知更完整，会区分添加、跳过、移除、保留、文件名同步和海报补全
- 优化前端提示文案，隐藏内部开发、构建和原始错误信息，改为用户可理解的场景化提示
- 清理生产环境调试日志，减少路径、链接和接口返回内容暴露

### 修复
- 修复批量检查更新确认移除后可能需要第二次检查才生效的问题，现在按视频集 ID 精准处理
- 修复批量检查更新无资源变更时仍弹出“发现变更”的问题
- 修复单个视频集检查更新取消勾选新发现分集后，又被后续刷新流程加入的问题

## v1.7.96 (2026-07-06)

### 优化
- 清理项目中已废弃的后端解析、HTTP 封装、全局快捷键和下载扩展逻辑，减少无效代码负担
- 优化播放器打开和切集体验：新开播放器先显示黑底，内部切集不再销毁重建窗口
- 编辑视频集和单视频海报时，选择新图片后立即预览，不用等保存后才看到效果

### 修复
- 保持库页面“全量检查更新”和设置页“批量更新海报”职责分离，避免资源检查流程误更新海报

## v1.7.95 (2026-07-05)

### 修复
- 修复库页面顶部“全量检查更新”入口被误改为“更新海报”的问题：现在恢复为检查新增/移除资源，不再更新已有视频集海报
- 批量更新海报继续只保留在设置页，避免资源同步和海报维护混用

## v1.7.94 (2026-07-05)

### 修复
- 修复本地海报路径正确但海报缓存为空时界面仍不显示的问题：缩略图生成失败时会回退读取原图生成 data URL，不再留下空 海报缓存
- 修复海报方向读取失败时方向为空的问题：无法读取尺寸时默认按横图处理，避免暂无资源视频集卡片展示异常

## v1.7.93 (2026-07-05)

### 修复
- 修复动漫分类扫描路径本身就是暂无资源视频集文件夹时无法被识别的问题：没有子文件夹且只有海报、没有视频文件时，也会作为视频集建立并写入自己的海报
- 修复大类检查更新对这种根扫描路径空视频集的漏报，后续检查更新可识别并刷新这类动漫暂无资源视频集

## v1.7.92 (2026-07-05)

### 修复
- 修复暂无资源视频集海报更新仍可能失败的问题：单个检查、详情页检查、大类更新海报、设置页批量修复都统一按 folder_path、标题、车牌三层定位真实视频集文件夹
- 修复大类检查更新对空视频集文件夹的误判：先解析真实文件夹再判断缺失，并用车牌去重，避免已有空文件夹被识别为新发现或本地已删除

## v1.7.91 (2026-07-05)

### 修复
- 修复暂无资源视频集海报更新定位问题：按“视频集就是文件夹”的模型处理，folder_path 不可用时会在对应大类扫描路径下按视频集标题寻找同名文件夹
- 批量更新海报现在同样覆盖无视频文件的空视频集文件夹，只要文件夹内有图片即可识别并更新海报

## v1.7.90 (2026-07-05)

### 修复
- 修复批量更新海报仍会跳过已有海报匹配不准确的问题：现在会重新按视频集自己的文件夹匹配海报，发现本地海报变化或缓存缺失都会更新
- 修复暂无资源的视频集单独检查更新仍不能稳定更新海报的问题：海报更新不再依赖视频扫描结果，只要视频集文件夹存在且有图片即可更新

## v1.7.89 (2026-07-05)

### 新增
- 设置页批量更新海报新增状态显示：可查看更新中、更新成功、更新失败，以及本次已检查和已更新的视频集数量

### 修复
- 修复暂无资源的视频集单独检查更新不能更新海报的问题
- 优化播放器启动显示：减少原生播放器窗口先透明再出画面的闪烁

## v1.7.88 (2026-07-05)

### 修复
- 修复批量修复海报只处理暂无海报的问题：现在可以批量识别并修复被上级演员、标签或时期目录污染的海报匹配不准确

### 优化
- 优化全量检查更新速度：检查已有视频集更新时改用轻量扫描，只比较视频文件增删和基础分集信息，不再读取图片或生成海报缓存

## v1.7.87 (2026-07-05)

### 修复
- 修复全量检查更新仍会覆盖已有视频集海报的问题：检查更新现在只同步视频新增、删除和分类关联，不再改动已有视频集海报
- 修复演员、标签、时期目录下新发现视频集的海报识别：只使用视频集自己文件夹内的海报，不再继承父目录海报，避免多个无季视频集被写成同一张图

## v1.7.86 (2026-07-05)

### 修复
- 修复视频页搜索输入时页面跳动/像刷新的问题：搜索和筛选更新 URL 参数时不再触发整页滚动恢复、整页入场动画重播或无意义的路由刷新
- 保留正常页面切换回顶部、返回/前进恢复位置、筛选状态回传和列表本地筛选能力

## v1.7.85 (2026-07-05)

### 修复
- 修复普通切换页面时恢复旧滚动位置的问题：点击侧边栏或页面入口会回到顶部，只有返回/前进时才恢复上次浏览位置

## v1.7.84 (2026-07-05)

### 修复
- 修复分类检查更新和确认新增的识别规则：分类开了演员就只识别演员，开了标签就只识别标签，两者都开时先演员后标签，未开启的能力不再参与误匹配
- 修复分类未开启演员/标签时的视频集识别：分类下一层直接按视频集处理，并保留车牌、中文字幕和普通视频集识别
- 修复分类下匹配不到演员或标签的文件夹仍显示为“新发现”的问题
- 修复视频页搜索输入实时查询时反复触发视频列表刷新的问题
- 修复多季/剧场版识别：明确包含“剧场版/篇”的目录按剧场版处理，明确包含 S1/S2 等季标识的目录按季处理，不明确时保留原数量规则
- 修复多季视频集海报识别：根目录没有海报时，会从季文件夹中寻找可用海报
- 修复中文字幕文件名更新识别：JUR-472 改名为 JUR-472-C/JUR-472-CH，或 JUR-472-AI 改名为 JUR-472-AI-C 时，识别为原视频的字幕更新，不再识别为新视频和本地已删除

## v1.7.83 (2026-07-05)

### 修复
- 修复视频页搜索框输入时触发页面刷新/重新初始化的问题
- 修复检查更新识别演员时期文件夹：时期文件夹只作为容器，不再识别为新视频集
- 修复演员目录和演员详情页导入空作品文件夹的问题：暂无视频的作品文件夹也可以作为视频集新增

### 优化
- 优化检查更新弹窗文案：用“新发现/已移除”替代“新增/本地已删除”，确认提示改为添加新发现内容、移除本地已删除记录

## v1.7.82 (2026-07-05)

### 新增
- 设置 > 数据存储新增“批量修复海报”：点击后后台静默修复缺失的视频集海报、分集海报和缓存，不阻塞继续使用

### 优化
- 全量检查更新不再承担海报回填和 海报缓存生成，专注检测新增/本地已删除资源，减少扫描时的图片处理开销

## v1.7.81 (2026-07-05)

### 修复
- 修复播放器后台有声无窗口问题：播放器窗口创建后立即显示并聚焦，不再依赖 mpv 尺寸事件触发首次显示
- 优化播放器窗口比例调整：打开前按数据库视频宽高计算窗口尺寸，真实尺寸事件只在比例差异明显时做一次微调，避免明显跳动
- 补齐前序版本的检查更新识别修复

## v1.7.80 (2026-07-05)

### 修复
- 修复检查更新识别逻辑：允许当前分类下无视频文件但有效的视频集目录被识别，不再因为只检查直接视频文件而漏掉需要更新的视频集

## v1.7.79 (2026-07-05)

### 修复
- 修复播放视频后 mpv 只有后台进程和声音、播放器窗口不显示的问题：不再使用 Windows 后置捞窗口，改为让 mpv 原生窗口启动即显示、置顶、非最小化并按主窗口位置打开
- 修复全量检查更新会清空正常视频集海报的问题：未匹配到新海报时保留旧海报，不再写空
- 修复检查更新不能恢复视频集海报的问题：视频集海报或缓存为空时，从本地视频集文件夹重新匹配海报并回填
- 补齐前序版本的稳定性修复

## v1.7.78 (2026-07-05)

### 修复
- 优化 Windows 播放器兼容性，提升播放器窗口打开稳定性
- 优化播放器窗口打开方式，减少有声音但窗口未显示的情况
- 修复全量检查更新会清空正常视频集海报的问题：未匹配到新海报时保留旧海报，不再写空
- 修复检查更新不能恢复视频集海报的问题：视频集海报或缓存为空时，从本地视频集文件夹重新匹配海报并回填
- 补齐前序版本的稳定性修复

## v1.7.77 (2026-07-05)

### 修复
- 优化 Windows 播放器兼容性
- 优化播放器窗口激活逻辑
- 补齐前序版本的稳定性修复

## v1.7.76 (2026-07-05)

### 修复
- 补齐前序版本的稳定性修复：修复检查更新识别不准确演员时期/中间目录、叶子视频集识别、分集海报补全、搜索防抖、视频集集数刷新、分类能力配置兼容与旧版本数据兼容、已完结展示、筛选不触发滚动、车牌/中文字幕功能解耦、简介/演员选择器展示条件、无海报品牌图兜底
- 修复 Windows mpv 播放窗口自动激活相关 Windows 兼容性，使用 windows 0.61 正确 BOOL 路径
- 修复部分版本更新记录展示异常

## v1.7.74 (2026-07-04)

### 修复
- mpv播放窗口自动激活到前台（EnumWindows返回BOOL+WNDENUMPROC传参）
- 检查更新后视频集集数自动刷新
- 分类分类能力配置兼容（补充缺失字段默认值+回填历史数据）
- 首页已完结视频集展示统一为全x话
- 筛选不触发页面滚动
- 车牌和中文字幕独立于演员功能，只依赖文件名识别
- 编辑模式演员选择器仅依赖分类能力配置.actors
- 简介textarea独立于所有功能开关
- 视频无海报时用品牌logo替代默认图标

## v1.7.73 (2026-07-04)

### 修复
- mpv播放窗口自动激活到前台（EnumWindows回调类型修复）
- 检查更新后视频集集数自动刷新
- 分类分类能力配置兼容（补充缺失字段默认值+回填历史数据）
- 首页已完结视频集展示统一为全x话
- 筛选不触发页面滚动
- 车牌和中文字幕独立于演员功能，只依赖文件名识别
- 车牌仅在文件名识别出时显示
- 编辑模式演员选择器仅依赖分类能力配置.actors
- 简介textarea独立于所有功能开关
- 视频无海报时用品牌logo替代默认图标

## v1.7.72 (2026-07-04)

### 新增
- 车牌和中文字幕独立于演员功能，只依赖文件名识别
- 车牌仅在文件名识别出时显示，自动填入
- 首页已完结视频集展示统一为全x话

### 修复
- mpv播放窗口自动激活到前台，避免变成后台进程
- 检查更新后视频集集数自动刷新
- 分类分类能力配置兼容（补充缺失字段默认值+回填历史数据）
- 筛选不触发页面滚动（仅pathname变化时处理）
- 详情模式下车牌展示独立于演员配置
- 编辑模式演员选择器仅依赖分类能力配置.actors
- 简介textarea独立于所有功能开关
- enum_windows_callback类型签名修复

## v1.7.71 (2026-07-04)

### 修复
- Windows mpv播放窗口自动激活到前台，避免变成后台进程

## v1.7.70 (2026-07-04)

### 修复
- Windows mpv播放窗口自动激活到前台，避免变成后台进程
- 检查更新识别演员时期文件夹，不识别为视频集
- 检查更新只报告叶子视频集（直接含视频且无子目录）
- check_series_updates跳过已有海报的视频不覆盖
- 搜索200ms防抖

## v1.7.69 (2026-07-04)

### 修复
- Windows mpv播放窗口自动激活到前台，避免变成后台进程
- 检查更新识别演员时期文件夹，不识别为视频集
- 检查更新只报告叶子视频集（直接含视频且无子目录）
- check_series_updates跳过已有海报的视频不覆盖
- 搜索200ms防抖

## v1.7.68 (2026-07-04)

### 修复
- 检查更新识别演员时期文件夹（如凪光/有栖花绯），不识别为视频集
- 检查更新只报告叶子视频集（直接含视频且无子目录），避免中间目录识别不准确
- check_series_updates跳过已有海报的视频不覆盖
- 搜索200ms防抖

## v1.7.67 (2026-07-04)

### 修复
- 构建构建失败（unused variable + syntax error）

## v1.7.66 (2026-07-04)

### 新增
- 页面切换滚动位置：导航到新页面滚顶部，返回恢复原位置
- 检查更新弹窗支持选择性更新（checkbox默认全选，可单独取消）
- scan_category识别演员时期文件夹，自动关联时期
- 视频页状态筛选新增未看完
- 启动时自动回填空display_type为anime

### 修复
- 检查更新全部识别为新发现内容（display_type空字符串+路径规范化+文件夹名匹配）
- 详情页未观看不显示提示文案
- 播放按钮图标优化（SVG三角形替代文字▶）
- add_video_series写入display_type，防止新建视频集display_type为空

## v1.7.65 (2026-07-04)

### 新增
- 检查更新弹窗支持选择性更新（checkbox默认全选，可单独取消新增/本地已删除的视频集和分集）
- scan_category识别演员时期文件夹，自动关联时期（period_id）
- 视频页状态筛选新增未看完（追番+未看完，与已看完互斥）
- 启动时自动回填空display_type为anime

### 修复
- 检查更新全部识别为新发现内容（display_type空字符串+路径规范化+文件夹名匹配）
- add_video_series写入display_type，防止新建视频集display_type为空
- 检查更新自动判断文件夹层级（标签/演员/视频集/分类）

## v1.7.64 (2026-07-04)

### 新增
- 视频页状态筛选新增未看完（追番+未看完，与已看完互斥）
- 启动时自动回填空display_type为anime

### 修复
- 检查更新全部识别为新发现内容（display_type空字符串+路径规范化+文件夹名匹配）
- add_video_series写入display_type，防止新建视频集display_type为空
- 检查更新自动判断文件夹层级（标签/演员/视频集/分类）

## v1.7.63 (2026-07-04)

### 修复
- 检查更新自动判断文件夹层级（标签/演员/视频集/分类），不再依赖 分类能力配置 硬编码

## v1.7.62 (2026-07-04)

### 修复
- 检查更新识别标签/演员子目录下的视频集文件夹（递归扫描一层）

## v1.7.61 (2026-07-04)

### 新增
- 检查更新支持分集海报识别（为没有海报的分集自动补上）
- 视频页右键追番/已看完（先关闭菜单再异步执行，不卡顿）
- 标签彩色气泡（12色，基于ID哈希）
- 演员字段标签区分系统预设/扩展预设/自定义
- 暂无分集显示暂无资源

### 修复
- 检查更新识别文件夹名 1-N 后缀变化视为同一视频集
- 重新识别时会保留手动修改过的标题
- 分类配置删除视频改为删除所有视频
- 删除设置页识别信息统一由检查更新处理

## v1.7.60 (2026-07-04)

### 新增
- 检查更新弹窗：新增/本地已删除分集确认后执行
- 标签彩色气泡（12色）
- 视频页右键追番/已看完
- 演员字段标签区分系统预设/扩展预设/自定义

### 修复
- 删除设置页识别信息，统一由检查更新处理
- 重新识别时会保留手动修改过的标题
- 暂无分集显示暂无资源

## v1.7.59 (2026-07-04)

### 修复
- 分集海报识别修复：same_dir helper 统一路径比较
- 新增连载状态(分类能力配置.status)控制开关
- 设置页大类→分类文案统一

### 新增
- 批量识别信息支持检测视频集/分集本地资源已删除

## v1.7.58 (2026-07-04)

### 修复
- 游戏覆盖设置改为标题在卡片外，间距与其他设置统一

## v1.7.57 (2026-07-04)

### 修复
- 视频页每次进入时刷新series，更新卡片观看状态

## v1.7.56 (2026-07-04)

### 修复
- 演员详情编辑态输入框重叠：grid改为minmax自适应列宽
- 切换大类时清空所有筛选项（收藏/已看/搜索）

## v1.7.55 (2026-07-04)

### 修复
- 播放器窗口先隐藏，等视频尺寸调整后再显示（消除闪烁）
- 移除 d3d11-sync-interval=0 修复 gpu-next 重复帧
- 设置页重排+重命名+用户友好描述

## v1.7.54 (2026-07-04)

### 修复
- 排查gpu-next卡住：回退到v1.7.49配置+仅改vo=gpu-next

## v1.7.53 (2026-07-04)

### 修复
- 移除 no-config 参数排查播放卡住

## v1.7.52 (2026-07-04)

### 修复
- 设置图标改用SVG文件，移除CSS clip-path黑块
- hwdec=d3d11va-copy导致挂起，改回d3d11va + d3d11va-zero-copy=no
- 应用图标全部加22%圆角（安装程序+桌面+任务栏统一）

## v1.7.51 (2026-07-04)

### 优化
- 渲染引擎迁移: vo=gpu → vo=gpu-next (libplacebo)
- hwdec: d3d11va → d3d11va-copy (防 overlay 踩 decoder surface)
- HDR 直通: target-colorspace-hint + hdr-compute-peak
- 色彩管理: target-trc=gamma2.2 (SDR LCD 防偏暗)
- 设置页一键禁用游戏覆盖开关
- 捆绑 nvidiaProfileInspector.exe，零操作全自动

## v1.7.50 (2026-07-03)

### 修复
- 播放器闪退: isMountedRef + mpvOperationLock 全面防护
- 播放器: 最大化/全屏兼容（直接全屏，退出恢复最大化）
- 播放器: 进度条预览缩略图修复（移除 crossOrigin）
- 播放器: 暂停不强制展开导航栏，移除暂停中间标记

### 优化
- 导航栏瘦身: 标题栏 58px→42px，上下独立控制
- NVIDIA 深度规避: d3d11va-copy, gpu-dumb-mode, no-config
- 窗口等比缩放: 监听 dwidth/dheight 按视频比例调整
- 全屏: 支持鼠标靠近右侧展开选集，2s 无操作隐藏光标
- 选集: 右侧导航栏覆盖式，鼠标靠近展开离开收起
- 选集: 简化展示（第几话+上次观看时间）
- 搜索: 图标重绘+融入窗口控件组+动画展开输入框
- 设置图标: 重绘为经典齿轮形状
- 返回页面: 恢复滚动位置
- 下拉框: 剧集单位+字段类型改为气泡展开选择

## v1.7.49 (2026-07-03)

### 修复
- 返回顶部: 修复 .changli-main 容器滚动定位
- 选集: 右侧导航栏式覆盖面板，鼠标靠近展开离开收起
- 选集: 简化展示，只显示第几话+上次观看时间
- 导航栏: 上下独立控制，鼠标靠近顶部只展示标题栏
- 导航栏: 鼠标离开立即隐藏
- 暂停: 移除中间暂停标记，不强制展开导航栏

## v1.7.48 (2026-07-03)

### 修复
- 播放器闪退: destroy/init 竞态修复
- 播放器闪退: NVIDIA 驱动识别规避 (vo=gpu, gpu-api=d3d11)
- 播放器闪退: mpv 健康检查崩溃恢复

### 优化
- 选集面板: CSS transition 平移渐入动画
- 选集缩略图: 展示实际视频画面
- 多季视频: 按季分层展示
- 选集边缘箭头触发
- 播放器音量记忆 (localStorage)
- 续播: loadfile 后等待 800ms 再 seek
- 视频集详情页: 添加刷新+返回顶部
- 窗口启动居中 (center: true)

# Changelog

## v1.7.47 - 2026-07-03

### 修复
- 设置页演员管理恢复虚线“新增字段”入口，并修复扩展系统预设关闭后仍出现在字段列表的问题，普通系统预设不受影响。
- 设置页大类管理的“剧集单位”下拉框改为项目统一的胶囊下拉样式。
- 主内容区路由切换时自动回到顶部，避免新页面继承上一页滚动位置。
- 主程序启动时仅短暂置顶拉到前台，随后立即取消，避免变成始终置顶。
- 播放器窗口按主程序窗口大小和视频比例自适应打开，高分辨率限制窗口大小，低分辨率自动放大。
- 播放器上下导航栏改为离开热区 1 秒淡出，关闭播放器时自动唤回主程序到最前方但不保持置顶。
- 播放器进度条悬停不再跳转播放位置，只有点击才 seek；悬停时尝试展示对应时间戳静态预览画面。
- 主程序和播放器最大化图标按窗口状态切换：窗口化为单矩形，最大化后为双矩形重叠。

### 验证
- `构建流程`
- `cargo check`

## v1.7.46 - 2026-07-03

### 修复
- 播放器窗口打开时改为居中到主窗口中间，避免新窗口跑到屏幕默认中心。
- 播放器视频区域取消上下预留边距，顶部标题栏和底部控制栏改为覆盖在视频上下侧。
- 播放器控制栏改为鼠标靠近顶部/底部展示，停留热区持续展示，离开后 3 秒淡出。
- 小窗模式保持视频比例并填满小窗，底部控制栏改为全透明覆盖层，保留右上角退出小窗和关闭入口。
- 修复最大化窗口下点击全屏仍被任务栏工作区限制的问题。
- 合并当前工作区里的视频库/演员库响应式布局、页面动效、窗口控件和详情观看进度文案等既有改动。

### 验证
- `构建流程`
- `cargo check`

## v1.7.45 - 2026-07-03

### 修复
- 修复应用右侧出现原生滚动条和自定义滚动条并存的问题，只保留主内容区自定义滚动条。
- 视频库、演员库等响应式卡片网格改为居中排列，窗口宽度不足以增加下一列时左右留白保持均衡。
- 首页“我的追番”和各大类预览固定只展示一行，窗口缩小后不会把多出的卡片换行露出。
- 重绘顶部设置、最小化、最大化、关闭按钮，设置按钮与窗口控制组统一，最大化改为双方形重叠图标。
- 优化进入视频页时标题、筛选卡片、搜索卡片和卡片列表的入场动画顺序，减少跳动和不连贯。

### 验证
- `构建流程`
- `cargo check`

## v1.7.44 - 2026-07-03

### 修复
- 首页“我的追番”和“我的大类”改为固定卡片宽度的响应式网格，窗口变宽只增加完整卡片数量，不拉伸卡片。
- 首页“热门演员”保留原左右滚动和箭头，同时使用固定 250px 横向列宽，窗口变大时可见卡片自然变多且不拉伸。
- 播放器 UI 保持新设计，不回退原生播放器；按 v1.7.33 前可播放基线加固 窗口/libmpv 显示层透明链路，避免根层/伪背景/stage 裁剪遮挡视频画面。
- 播放器小窗增加退出小窗和关闭入口，压缩小窗控制栏尺寸；选集入口调整到前进 10 秒后方；全屏暂停标固定居中。

### 验证
- `构建流程`
- `cargo check`

## v1.7.43 - 2026-07-03

### 修复
- 全局内容页改为随窗口宽度自动增加/减少每行卡片数量，并保持原有卡片比例和基准尺寸。
- 最大化后收窄左右留白，主内容区铺满窗口可用宽度。
- 页面滚动条改为从顶部导航栏下方开始，不再顶到导航栏。
- 顶部设置/最小化/最大化/关闭按钮统一为品牌化窗口控制组，移除外层气泡并保持靠右上角。
- 视频集详情播放提示在已看完状态下仍显示真实上次观看集数，并移除右键海报说明小字。
- 设置页大类上移/下移后同步刷新视频页大类顺序，视频页默认进入当前排序首位大类。

### 验证
- `构建流程`
- `cargo check`

## v1.7.42 - 2026-07-03

### 修复
- 安装器稳定切换为圆角 Region 裁剪方案，并在 shell 边缘增加内侧柔光、渐变和阴影以淡化硬裁锯齿，不再使用易回退白底的纯透明缓冲方案。
- 视频页 B2 动效回滚：恢复大类标题原始选中放大和页面入场动画。
- 视频页 B1 响应式按原固定列数为基础追加大屏断点，默认尺寸保持横版 4 列、竖版 5 列，窗口更大时再增加列数。
- 视频集详情页：海报右键菜单承载编辑信息、管理季、检查更新；空简介显示“暂无简介”；已看完仍展示上次观看进度；播放 CTA 与进度文案调整到更接近视频客户端的位置和尺寸。

### 验证
- `构建流程`
- `cargo check`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `CHANGLI_NSIS_SETUP="$PWD/package.json" cargo check --manifest-path installer-shell/Cargo.toml`
- `CHANGLI_NSIS_SETUP="$PWD/package.json" cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.41 - 2026-07-03

### 修复
- 撤回安装器 v1.7.40 的 Region/GDI 硬裁兜底，恢复纯透明链路。
- 移除 `CreateRoundRectRgn` / `SetWindowRgn` 和 `Windows_Graphics_Gdi` 依赖，避免切割像素导致锯齿硬边。
- 恢复外层 Tao 透明窗口 `1000x660` + 安装器窗口 子窗口 `(10,10) 980x640` 架构，让圆角和阴影由 CSS/安装器窗口 自然抗锯齿渲染。
- 保留 layered + DWM full-client 透明处理，只用于宿主窗口透明合成。
- 安装进度动画从原速度 1.8 倍调整为 2 倍。

### 验证
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `CHANGLI_NSIS_SETUP="$PWD/package.json" cargo check --manifest-path installer-shell/Cargo.toml`
- `CHANGLI_NSIS_SETUP="$PWD/package.json" cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.40 - 2026-07-03

### 修复
- 修复自定义安装器真实 Windows artifact 仍露出白色矩形外框的问题。
- 安装器外层窗口尺寸改为与内部 shell 同尺寸 `980x640`，去掉此前 `1000x660` 父窗口与 `(10,10)` 窗口 偏移带来的透明缓冲区白底风险。
- 保留 layered + DWM full-client 透明处理，并新增 Windows 圆角 region 兜底裁剪，覆盖父窗口透明、安装器窗口 透明或截图背景回退为白底等多种原因。

### 验证
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `CHANGLI_NSIS_SETUP="$PWD/package.json" cargo check --manifest-path installer-shell/Cargo.toml`
- `CHANGLI_NSIS_SETUP="$PWD/package.json" cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.39 - 2026-07-03

### 修复
- 精准修复视频页进入动效：大类标题直接以固定大字展示，不再参与选中放大/缩放动画；页面上提与卡片入场动画恢复原节奏。
- 恢复视频集详情页主播放按钮尺寸，并把“上次观看到…”/“尚未观看”提示放到按钮下方展示，避免被截断。
- 播放器恢复透明 窗口/libmpv 渲染路径，避免 CSS/窗口 背景盖住视频层；mpv 初始化参数恢复到 `gpu-next`/`auto-safe` 路线。
- 优化播放器中间播放提示图标，去掉廉价字符白圈效果，改为 CSS 图标与深色半透明磨砂按钮。
- 调整播放器底部按钮顺序为播放/暂停、后退、下一集、前进，并继续移除“第几话”文字。
- 小窗模式底部控制条改成居中胶囊，只保留主要操作区域，隐藏复杂功能。

### 验证
- `构建流程`
- `cargo check`

## v1.7.38 - 2026-07-03

### 修复
- 播放器窗口改回实底播放器界面，避免透明 窗口 遮罩导致有声音无画面或边缘发白。
- 播放器顶部窗口按钮改为稳定图标绘制，并修复最大化/全屏/小窗状态切换冲突。
- 播放器分集列表默认收起，改为手动展开侧栏，底部控制区保留播放、快退、快进、下一集、倍速、音量、分集、小窗、全屏。
- 视频库大屏布局扩展到 1800px，并使用自适应网格，避免 4K 屏只显示少量卡片。
- 视频库切换动效降级为轻量动画，减少大列表卡顿与标题跳动。
- 视频集详情页隐藏无演员配置的演员区，状态与集数气泡独立显示，并优化品牌播放按钮提示。
- 安装器窗口透明层改为 Windows layered + DWM full-client frame，继续压掉圆角外白底。

### 验证
- `构建流程`
- `cargo check`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `CHANGLI_NSIS_SETUP="$PWD/package.json" cargo check --manifest-path installer-shell/Cargo.toml`

## v1.7.37 - 2026-07-03

### 修复
- 修复重新识别信息不会提示磁盘已删除分集的问题，检查更新后弹窗列出本地已删除分集并需用户确认后删除应用内记录。
- 移除 窗口 默认全局右键菜单，同时保留视频集/海报等局部右键菜单。
- 修复主窗口和播放器窗口自定义窗口控件权限与拖动事件，恢复最小化、最大化、关闭、拖动、全屏/小窗能力。
- 修复播放器只有声音无画面的问题，透明化播放器 窗口 背景并避免假视频层遮挡 mpv 画面。
- 接通播放/暂停状态反馈，移除置顶、字幕、记录进度等冗余播放器 UI。
- 优化视频集详情页播放入口位置，添加“检查更新”，并移除重复“上次观看到”文案。
- 优化播放器分集列表：默认收起、支持展开、只显示简化集数和播放状态。
- 将继续观看提示改为 5 秒渐入渐出通知。
- 视频页大类配置改为启动预加载并复用 store 快照，减少跳转后标题和筛选区闪现。

## v1.7.36 (2026-07-03)

### 修复
- 安装器透明圆角方案移除 Windows GDI 原生裁剪，不再使用 `CreateRoundRectRgn` / `SetWindowRgn` 裁切窗口，避免硬裁剪带来的圆角锯齿、白色线条和白底反复问题。
- 安装器继续保留透明父窗口 + 内缩 安装器窗口 + CSS `.shell` 圆角主体结构，由浏览器渲染可见圆角和阴影。

### 验证
- `构建流程`
- `cargo check`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.35 (2026-07-03)

### 修复
- 恢复 PageMotion 到 v1.7.32 原有页面入场动效，撤回 v1.7.34 对视频库/演员页动效范围的降级，避免改没原来效果。

### 优化
- 安装器安装进度条速度调整为原来的 1.8 倍：进度刷新间隔从 180ms 改为 100ms。

### 验证
- `构建流程`
- `cargo check`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.34 (2026-07-03)

### 修复
- 修复 v1.7.33 大列表路由切换明显卡顿：视频库、视频集详情和演员页不再对所有卡片逐个跑 GSAP 入场动画，只保留轻量淡入。
- 修复无边框主窗口右上角最小化、最大化、关闭按钮点击无效：改为 JS 控制窗口拖动区域，避免整条导航栏吞掉按钮点击。
- 修复播放器多开窗口缺少 Tauri capability 导致窗口控制和播放器 API 失效：动态 `player-*` 窗口获得 core/libmpv 权限。
- 安装器透明外壳增加 Windows 圆角区域裁剪兜底，配合透明父窗口和透明 安装器窗口 子窗口，防止 安装器窗口 白色底层继续从圆角外漏出。
- 重新识别信息现在会重新扫描视频集文件夹并把新增视频文件补入当前视频集，适配连载作品更新集数。

### 优化
- 播放器第一版功能闭环：播放/暂停、下一集、1x/1.5x/2x/3x 倍速、音量、全屏、分集列表和选集播放。

### 验证
- `构建流程`
- `cargo check`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`
- `npm run tauri:build`

## v1.7.33 (2026-07-03)

### 修复
- 安装器透明外壳继续去白底白边：父窗口保持透明，安装器窗口 子窗口显式透明背景，并移除 DWM 圆角偏好，避免系统圆角与透明窗口叠加补白。
- 视频页从视频集详情返回时缓存分类标签、演员和筛选结果，减少演员/标签筛选栏重新加载导致的卡顿。

### 优化
- 视频集详情页移除观看进度大卡片，恢复总集数小气泡，观看进度改成“上次观看到第几季第几话/集/部”的小字提示。
- 视频集详情页“立即观看/继续观看”调整到标题信息区右侧，避免挤占详情信息布局。
- 视频集编辑态追番状态改为“连载中 / 已完结”品牌分段开关，不再使用原生下拉框。
- 主程序窗口去除系统默认标题栏，把最小化、最大化、关闭集成到 ChangLi 导航栏右侧，并支持导航栏拖动窗口。

### 验证
- `构建流程`
- `cargo check`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`
- `npm run tauri:build`

## v1.7.32 (2026-07-02)

### 修复
- 视频页进入视频集后返回时保留当前大类、筛选条件和搜索关键词，避免从动漫返回时跳回默认影视大类。
- 动漫等非成人向视频集右键“重新识别信息”不再要求番号格式，可按文件夹名和海报正常刷新。
- 修复标签、演员、状态筛选胶囊 hover 上移时被筛选行顶部裁切的问题。

### 优化
- 视频集详情页新增观看进度卡片，展示尚未观看、看到第几话/集/部、已看完等状态。
- 多季视频集的观看进度精确到第几季第几话/集/部，并同步到首页、视频页和视频集详情页。
- 视频集详情页右侧新增品牌主按钮：尚未观看显示“立即观看”，有观看记录显示“继续观看”，并从第一集或最近播放集打开播放器。
- 视频集编辑态的追番状态下拉框改为 ChangLi 标准表单下拉样式。

### 验证
- `构建流程`
- `cargo check`
- `npm run tauri:build`


## v1.7.31 (2026-07-02)

### 优化
- 安装器透明外壳改为父窗口透明 + 安装器窗口 子窗口内嵌：外层窗口保持 1000×660，安装器窗口 仅放置在内部 10px 偏移处的 980×640 区域。
- 移除 HTML 主体自身的 10px margin，避免 安装器窗口 继续覆盖透明缓冲区导致圆角外白底；圆角与阴影仍由内部 `.shell` 绘制。
- 保留 安装器窗口 数据目录固定到 `%LOCALAPPDATA%/ChangLi/Installer安装器窗口`。

### 验证
- `构建流程`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`
- `npm run tauri:build`

## v1.7.30 (2026-07-02)

### 优化
- 安装器恢复透明窗口路线：窗口和 窗口 重新启用透明，`html/body` 改回透明背景。
- 外层窗口扩大到 1000×660，内部 980×640 安装器居中保留 10px 透明缓冲区，让 CSS 圆角和阴影有抗锯齿空间，避免圆角外填充白底。
- 保留 安装器窗口 数据目录固定到 `%LOCALAPPDATA%/ChangLi/Installer安装器窗口`，避免在安装器当前目录生成运行数据文件夹。

### 验证
- `构建流程`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`
- `npm run tauri:build`

## v1.7.29 (2026-07-02)

### 优化
- 安装完成后的按钮文案改为“完成”和“完成并启动”。
- 修复 v1.7.28 圆角处理导致四角和底部漏出白底的问题，窗口改回非透明实底并使用 Windows DWM 原生圆角偏好。
- 固定安装器 安装器窗口 数据目录到 `%LOCALAPPDATA%/ChangLi/Installer安装器窗口`，避免安装器启动时在当前目录生成运行数据文件夹。

### 验证
- `构建流程`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.28 (2026-07-02)

### 优化
- 安装器左侧文案、INSTALLER 标识、说明小字、三个功能标签、品牌副标题位置和右上版本号继续按旧预览稿对齐。
- 安装位置区域移除小房子图标，并改为预览稿同款浅灰内嵌卡片与白底“更改”按钮。
- 右上步骤条横条缩短，接近预览稿的一横两点样式。
- 左侧背景横竖网格线条改稀疏，竖线约 9 条，横线按比例同步减少。
- 优化安装器四角圆角：移除 Windows GDI 硬裁剪，改用透明窗口与 CSS 圆角裁切，圆角提高到 34px 以减少锯齿感。
- 安装完成后的“仅关闭”改为“关闭”。

### 验证
- `构建流程`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.27 (2026-07-02)

### 优化
- 安装器继续压掉窗口白色描边，外壳使用实底、圆角裁切和更克制的右侧背景高光，减少边缘白线感。
- 点击开始安装后，安装位置和 1/2/3 卡片以右移、淡出、轻微旋转和模糊的动效退场。
- “准备安装长离”加入物理下落动效，安装时落到中间醒目位置并显示“正在安装长离中”与规律跳动的三个点。
- 安装完成后主标题切换为“安装成功”，完成按钮文案改为“关闭并打开”。
- 移除右侧说明文案、左侧安装说明和底部“准备就绪”，版本号移动到窗口底部居中并抬高，避免被进度条遮挡。

### 验证
- `构建流程`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.26 (2026-07-02)

### 优化
- 安装器打开改为页面连续两帧渲染完成后再显示窗口，关闭和关闭并打开时先隐藏窗口再退出，进一步减少露出底层原始框架的几帧闪现。
- 安装器移除外圈白色描边和内高光白边，并调整 Windows native 裁剪圆角与页面圆角匹配，降低四角廉价白边感。
- 安装过程大标题加入状态切换动画，从“准备安装长离”切换为“正在安装中”，完成后切换为“安装成功”。
- 安装进度条上方小字按首次安装/更新安装展示真实过程文案，例如“检测到首次安装，请稍后”和“检测到已有版本，正在覆盖更新安装中”。
- 中间安装位置卡片和 1/2/3 步骤随安装状态联动，更新安装会展示检测旧版、覆盖更新、创建入口等阶段信息，完成后进入完成态。

### 验证
- `构建流程`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.25 (2026-07-02)

### 优化
- 安装器左侧品牌图标区域按旧预览稿补回白色渐变高光、半透明玻璃底、描边和柔和阴影，同时保持原图标视觉尺寸不缩小。
- 安装器左下装饰卡片按旧预览稿恢复为三张半透明竖向海报卡，从底部裁切露出，重叠处可见后层轮廓。
- 安装器右侧 1/2/3 步骤卡按旧预览稿改为白底细边框卡片与淡粉小圆角编号，不再使用重高亮样式。
- 安装中进度条改为 1% 到 100% 递增显示，取消原来的充能式循环动画。

### 验证
- `构建流程`
- `cargo fmt --manifest-path installer-shell/Cargo.toml -- --check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.24 (2026-07-02)

### 优化
- 视频页移除进入时的整页骨架等待，改为立即渲染现有视频内容，并沿用页面进入的从下往上浮现动画。
- 提升 Windows 自定义安装器品质：窗口启动先隐藏，圆角和 窗口 准备完成后再展示，减少打开时露出底层原始框架。
- 安装器关闭和完成后打开加入收尾动画，减少关闭时露出底层原始框架。
- 安装器改回品牌图标资源，外壳 exe 写入 `src-tauri/icons/icon.ico`，避免显示默认图标。
- 安装器除按钮/输入等交互控件外，任意空白区域都可拖拽移动窗口。
- 安装完成后新增“关闭并打开长离”功能，同时保留“仅关闭”。
- 安装器左侧背景改为垂直线条与横线条网格，底部三张玻璃卡片改为左 45 度、中间垂直、右 45 度堆叠，并加强高光、玻璃、阴影和边缘清理。

### 验证
- `构建流程`
- `cargo check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.23 (2026-07-02)

### 修复
- 修复 Windows 安装器窗口 安装器按钮无响应：按钮改为 `changli://close` / `changli://choose-dir` / `changli://install` 链接，Rust 侧用 `with_navigation_handler` 拦截导航触发事件。
- 不再依赖 `window.ipc.postMessage`，规避真实 Windows artifact 中 JS→Rust IPC 不通导致的安装、取消、更改、关闭全部无反应问题。
- 点击安装后由 Rust 侧触发 `window.setInstalling()` 更新进度状态，再执行内层静默安装。

### 验证
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.22 (2026-07-02)

### 修复
- 修复 v1.7.21 安装器按钮点击后 Rust 侧无响应的问题：IPC 从 JSON 对象协议改为 Wry 官方示例同款纯字符串命令。
- 取消、关闭、更改安装位置、开始安装、完成后关闭全部改为 `window.ipc.postMessage('command')`。
- Rust 侧直接按 `drag` / `close` / `choose-dir` / `install` 字符串分发事件，避免 JSON stringify/parse 兼容问题。

### 验证
- 本地 HTML 预览中按钮点击会改变前端状态。
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.21 (2026-07-02)

### 修复
- 修复 安装器窗口 安装器 IPC 交互：关闭、取消、更改目录、开始安装统一发送 JSON 命令，避免按钮点击无响应。
- 默认安装位置优先读取旧版长离注册表卸载信息中的 `InstallLocation` / `DisplayIcon` 路径，找不到旧版时再回退到当前用户 AppData。
- 删除“安装后打开”和“开机自启”选项，避免安装器展示非必要能力。
- `ChangLi` 下方副标题改为中文“私人影音资料库”。
- 右侧 1/2/3 和顶部步骤条改为真实联动：准备阶段高亮检测位置，安装阶段高亮写入组件，完成阶段高亮完成状态，失败时显示失败阶段。
- 保留真实目录选择、窗口拖动、圆角裁剪、取消退出和 `/S /D=<目录>` 静默安装。

### 验证
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.20 (2026-07-02)

### 修复
- 修复 安装器窗口 自定义安装器与预览图结构不一致的问题：右侧标题/说明改回基础背景层，版本号移动到右上灰色弱化，顶部步骤指示改为彩色长条 + 两个灰点，安装位置与 1/2/3 步骤合并到同一个操作卡片。
- 修复安装器静态假功能：删除未实装的“保留本地数据/播放器就绪/自动建库”能力卡，改为真实安装步骤；未开始安装时不再显示进度条和“预计一分钟”。
- 修复窗口行为：安装器启动居中，原生圆角裁剪，窗口可拖动，取消/关闭可退出；安装中禁用取消，避免无效交互。
- 修复安装位置：启动时显示真实默认安装路径，点击“更改”打开目录选择器，安装时把用户选择路径传给内层 NSIS `/D=`。
- 实装“安装后打开”和“开机自启”选项：安装成功后按勾选项启动 ChangLi 或写入 HKCU Run 启动项。
- 安装状态改为绑定内层安装进程退出结果，成功/失败由真实 exit code 决定，不再使用 setTimeout 或随机进度假动画。

### 验证
- `构建流程`
- `cargo check`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.19 (2026-07-01)

### 优化
- 合并 更新 #24：将 Windows 自定义安装器外壳从 Windows GDI 自绘升级为 Tao + Wry/安装器窗口 渲染，安装器 UI 改用 HTML/CSS 实现，提升圆角、阴影、渐变、字体层级和卡片质感上限
- 保留“自定义外壳 + 内层 NSIS 静默安装”架构：用户打开 `ChangLi_<version>_x64-setup.exe` 时看到 安装器窗口 品牌安装器，点击开始安装后通过 IPC 触发内层 NSIS `/S` 安装
- 安装器左侧保留 rose/orange 品牌渐变、真实 app 图标、品牌海报文案和能力 pill；右侧保留安装路径控制项、品牌 tint 能力卡、渐变进度条和高光主按钮

### 验证
- `构建流程`
- `cargo check --manifest-path installer-shell/Cargo.toml --target x86_64-pc-windows-msvc`

## v1.7.18 (2026-07-01)

### 修复
- 合并 更新 #23：修复 Windows 自定义安装器窗口默认出现在屏幕左上角的问题，启动时按屏幕尺寸居中显示
- 修复安装器无法操作的问题：`WM_NCHITTEST` 不再全窗口返回 `HTCAPTION`，仅顶部空白区可拖拽，关闭/取消/开始安装按钮恢复正常点击
- 修复安装器四角无圆角的问题：使用 `CreateRoundRectRgn` + `SetWindowRgn` 裁剪真实 Windows 窗口

### 优化
- 按历史 `design/installer-preview.html` 方向扩大安装器为 980x640，重绘品牌左栏、主面板、卡片、按钮与进度区域，使外观更接近独立安装器预览稿

## v1.7.17 (2026-07-01)

### 新增
- 合并 更新 #22：新增独立 Rust/Windows 自定义安装器外壳，不再把自定义页面塞进 NSIS 默认 wizard 框架
- 自定义安装器在编译期嵌入内层 NSIS setup，点击开始安装后写入临时目录并静默执行 `/S`

### 优化
- Windows Release 最终资产保持 `ChangLi_<version>_x64-setup.exe` 命名，并用自定义安装器外壳覆盖 tauri-action 生成的内层 NSIS 资产，保证用户下载到的是新外壳
- 移除完整 NSIS template 文件和 tauri.conf 中的 `nsis.template` 配置，回退到内层 NSIS 只负责真实安装后端
- 更新安装器 UI 文档，明确当前架构是“自定义外壳 + 内层静默 NSIS”

## v1.7.16 (2026-07-01)

### 新增
- 合并 更新 #21：启用完整 Windows NSIS `template`，用 ChangLi 自定义安装首页/完成页替换默认欢迎页、路径页、开始菜单页和完成页
- 自定义安装首页包含品牌侧栏、真实 app 图标、安装位置卡片、更改路径按钮、能力卡和开始安装/取消按钮文案
- 自定义完成页包含安装完成摘要、安装路径、立即打开 ChangLi 勾选项和完成按钮

### 优化
- 删除主程序顶栏旧 `.changli-wordmark::before` 橙色伪图标，左上角只保留真实 app 图标 + 长离文本
- 更新安装器 UI 设计文档，记录完整 NSIS template 范围和 Windows 验证边界

## v1.7.15 (2026-07-01)

### 新增
- 合并 更新 #19：演员添加作品支持番号格式文件夹（如 `STARS-667C[标题]`）直接导入；选定时期添加作品时改为校验时期名称；错误提示显示期望名称或番号格式
- 合并 更新 #20：升级 Windows 安装器品牌视觉资源，重绘 NSIS header/sidebar，使用真实 ChangLi 图标；主程序顶栏左上角升级为 app 图标 + 长离文字；新增安装器 UI 落地文档

### 修复
- 合并 更新 #19：演员自定义字段禁用后不再隐藏，保留可重新启用入口
- 演员编辑生日支持保存为空；空生日不会再保存为 `0000-00-00`，非编辑态也不展示生日字段

## v1.7.14 (2026-07-01)

### 优化
- 合并 更新 #17：统一视频库筛选面板，将标签、演员、状态筛选整合到同一个 `changli-filter-panel`，去掉割裂的白色大卡片和深色投影，展开/收起改为更顺滑的 max-height 过渡
- 合并 更新 #18：提升视频详情页跳转响应，...[truncated]

### 修复
- 合并 更新 #14：修复设置页“检查更新”在用户端持续返回 GitHub Release API 403 的问题；改为 Tauri 后端请求 GitHub，并在 API 失败时 fallback 到 `/releases/latest` 重定向解析版本
- 合并 更新 #15：优化视频库跳转后的首屏渲染，等待大类和当前筛选项准备好后一次性展示真实页面，等待期间显示同结构骨架屏
- 合并 更新 #15：修复标签/演员筛选气泡收起状态底部和阴影被裁剪的问题
- 合并 更新 #16：移除视频库排序控件，保留标签、演员、已追番、已看完、中文字幕、搜索和批量选择等筛选功能

### 验证
- `构建流程`
- `cargo check`
- `npm run tauri:build`

## v1.7.12 (2026-07-01)

### 新增 / 优化
- 合并 更新 #11：继续优化桌面端交互细节，统一热门演员滚动箭头、视频库排序控件、详情页返回按钮、tooltip、选中态、chip 和角标等品牌细节
- 合并首页“我的追番 - 查看全部”跨大类聚合视图，进入 `/library?favorite=1&scope=all` 时展示全部已追番内容，不再被默认大类限制
- 热门演员名称下方显示演员别称，替代生日信息

### 修复
- 合并 更新 #13：修复新演员上传海报后缩略图和主海报不显示的问题，后端在 `photo_海报缓存` 为空时自动从文件路径读取图片并存入 DB
- 修复视频详情页数据未准备好时提前渲染的问题
- Toast 通知 success/info 改为白底黑描边，error 保持红色
- 演员编辑数值/车灯输入框父容器改为 inline-flex，让上下箭头紧贴输入框
- 检查更新 GitHub Release 请求补充 User-Agent 请求头，降低 403 风险

### 验证
- `构建流程`
- `cargo check`
- `npm run tauri:build`

## v1.7.11 (2026-07-01)

### 新增
- 合并 更新 #10：统一 ChangLi 桌面端视觉系统，新增 PageMotion 页面动效组件
- 引入 GSAP / @gsap/react，用于页面级细微动效

### 优化
- 首页、视频库、搜索、下载、标签、演员、详情页、设置页和播放器统一卡片、面板、弹窗、按钮、输入框、右键菜单和空状态样式
- 保留 更新 #10 原始 UI 升级设计，不额外改变视觉决策
- 保留 v1.7.10 的通知右侧滑入、演员海报同步、参演作品标题 hover 和更新确认弹窗修复

## v1.7.10 (2026-06-30)

### 修复
- 通知恢复为历史右侧滑入渐入样式，去掉绿色线条，统一为简洁黑底提示
- 新增/删除/设主演员海报后同步刷新演员页、演员列表和首页热门演员，第一张海报立即可见
- 参演作品标题保持最多两行省略，鼠标停留时可查看完整名称
- 检查更新发现新版时先弹窗确认，用户选择后再跳转下载安装包

## v1.7.9 (2026-06-30)

### 修复
- Toast 通知位置调整到右上角设置图标下方，并从右上角渐入
- 首页热门演员卡片恢复原始大尺寸，新增数量通过横向滚动展示，不再缩小卡片

## v1.7.8 (2026-06-30)

### 改进
- 设置页检查更新改为 GitHub Release API：不再依赖 Tauri updater 的 `latest.json`/`.sig`
- 检测到新版本时自动打开最新 Windows 安装包下载链接，用户按安装器完成更新
- 移除 Tauri updater 插件和权限残留，避免发布链路再被 updater 签名产物卡住

## v1.7.7 (2026-06-30)

### 新增
- 合并 更新 #7：演员详情页访问次数统计，演员列表按热门度排序
- 首页热门演员扩展为 10 个并支持横向滚动、箭头切换和查看更多
- 视频库默认展示排序第一的大类，不再硬编码 anime
- 演员参演作品名称支持 hover 查看完整标题

### 发布
- 暂时撤掉失败的 Tauri updater JSON 自动上传步骤，保证 Windows exe/msi 发布链路先恢复稳定；后续改为 GitHub Release API 下载更新方案

## v1.7.6 (2026-06-30)

### 修复
- 修复删除演员时期时报 `resource_actors.period_id` 字段不存在导致失败的问题
- 修复 Windows 播放页只有声音没有画面的问题，Windows 改用原生 mpv 播放路径
- 修复设置页检查更新因 Release 缺少 updater JSON/签名导致无法获取有效更新信息的问题

### 优化
- 设置页当前版本与版本更新记录改为构建时自动生成，避免手写遗漏
- 统一刷新、删除、检查更新等操作的通知样式和显示时长
- GitHub Actions 发布时显式上传 Tauri updater JSON 和签名文件

## v1.7.5 (2026-06-30)

### 新功能
- 演员详情页支持把已添加的视频/视频集重新分配到指定时期
- 新增 ChangLi 多会话并行开发流程文档和 PR 模板

### 修复
- 演员详情页手动刷新不再重置当前滚动位置
- 修复演员时期删除二次确认后无法删除的问题
- 修复重新扫描已有作品时时期不会更新的问题

### 优化
- 危险操作统一使用确认弹窗，替代原生 confirm 和右键二次点击混用
- 抽取通用确认弹窗组件和开关组件，统一 ActorDetail、Library、SeriesDetail、Settings 的交互样式
- 统一受影响页面的通知提示时长

## v1.6.6 (2026-06-29)

### 新功能
- 切换类型支持多大类选择
- 首页加刷新+返回顶部
- 刷新按钮反馈 toast
- 大类排序支持
- 生日/身高/体重/三围上下箭头
- 罩杯 A-Z 上下箭头

### 修复
- 体重编辑不生效
- 动漫标签筛选显示 0 个
- 演员详情页滚动到顶部
- 关闭扩展预设后详情态不展示
- 删掉 actor_fields 的 name 字段

### 优化
- 扩展预设弹窗隐藏类型标签
- 问号 tooltip 边缘自适应
- 去掉必填星号
- 刷新按钮位置固定
- 删掉 restore_preset_templates 迁移

## v1.6.4 (2026-06-29)

### 新功能
- 检查更新功能：设置页一键检查并自动更新
- 版本更新记录：设置页可查看完整版本历史

### 修复
- 预设模板恢复：缺失的预设模板自动补回
- 扩展预设关闭后不再显示在演员管理列表

### 优化
- 编辑字段弹窗移除格式配置下拉
- 构建 构建自动创建 GitHub Release

## v1.6.1 (2026-06-29)

### 优化
- 预设模板系统完善 + 定制功能实装

## v1.6.0 (2026-06-29)

### 新功能
- 预设模板系统
- 罩杯预设
- 右键删除二次确认

### 优化
- 名称字段去掉

## v1.5.0 (2026-06-29)

### 新功能
- 筛选简化
- 演员管理增强

### 优化
- 大类管理优化
- UI 改进

## v1.4.0 (2026-06-29)

### 新功能
- 9 项优化
- 筛选简化
- 大类管理增强

## v1.3.1 (2026-06-29)

### 修复
- 筛选栏标签和演员分两行独立展示

## v1.3.0 (2026-06-29)

### 新功能
- 一键扫描功能

### 优化
- 删除写死逻辑

## v1.2.1 (2026-06-28)

### 修复
- 筛选栏标签/演员兼容
- 视频按 display_type 筛选

## v1.2.0 (2026-06-24)

### 新功能
- 演员字段配置化
- 大类 分类能力配置 实装

## v1.1.0 (2026-06-22)

### 新功能
- 大类配置化
- 演员字段配置化

## v1.0.5 (2026-06-22)

### 修复
- 去掉自定义 NSIS 模板，用 Tauri v2 默认模板

## v1.0.4 (2026-06-22)

### 修复
- 版本号同步

## v1.0.3 (2026-06-22)

### 修复
- 构建 下载 mpv-dev 包获取 libmpv-2.dll
- 下线 migrate_actor_photos 一次性迁移函数

## v1.0.2 (2026-06-22)

### 修复
- NSIS 打包 libmpv DLL + 版本号同步
- 删除 tauri.conf.json v1 格式配置

## v1.0.1 (2026-06-22)

### 修复
- 构建 libmpv-wrapper.dll 路径修复

## v1.0.0 (2026-06-22)

### 新功能
- 内嵌 mpv 播放器
- 构建 libmpv 修复

## v0.9.4 (2026-06-22)

### 修复
- Tauri v2 插件注册 + capabilities 权限配置

## v0.9.3 (2026-06-22)

### 修复
- windows 0.61 HWND 类型适配

## v0.9.2 (2026-06-22)

### 修复
- windows 0.61 HWND 类型适配

## v0.9.1 (2026-06-22)

### 修复
- windows crate 升级到 0.61，匹配 Tauri 2.11.3

## v0.9.0 (2026-06-22)

### 修复
- Tauri v2 API import 修复

## v0.8.9 (2026-06-22)

### 修复
- Tauri CLI 升级到 v2，修复 构建 构建

## v0.8.8 (2026-06-22)

### 新功能
- Tauri v2 升级

## v0.8.7 (2026-06-22)

### 修复
- 切换主海报后同步更新 actors 表

## v0.8.6 (2026-06-22)

### 修复
- 展开按钮裁切修复
- 时期删除二次确认修复

## v0.8.5 (2026-06-22)

### 修复
- 旧数据海报一次性迁移
- 回滚特殊逻辑

## v0.8.4 (2026-06-22)

### 修复
- 旧数据海报修复
- FloatingActions
- 右键上下自适应

## v0.8.3 (2026-06-22)

### 新功能
- 右键菜单自适应
- 动漫文件夹模式

## v0.8.2 (2026-06-22)

### 修复
- 海报覆盖bug
- 右键移主海报
- 时期弹窗
- 空时期显示

## v0.8.1 (2026-06-22)

### 新功能
- 演员多张海报
- 时期功能重新设计

## v0.8.0 (2026-06-21)

### 修复
- 添加失败toast从右侧平移
- 删除确认自定义弹窗

## v0.7.9 (2026-06-21)

### 新功能
- 6项改动：删除修复、display_type判断、海报fallback、参演作品、影视模式、自动关联

## v0.7.8 (2026-06-21)

### 修复
- 严重 bug 修复：二次确认、扫描海报保留

## v0.7.7 (2026-06-21)

### 修复
- 动漫/影视判断改用 series_actors 实际关联

## v0.7.6 (2026-06-21)

### 修复
- 展开按钮检测修复
- 排序位置调整

## v0.7.5 (2026-06-21)

### 修复
- 详情页标签回滚到 v0.7.1 样式

## v0.7.4 (2026-06-21)

### 修复
- UI 样式修复：展开按钮、排序位置、影视已看完、详情标签回滚

## v0.7.3 (2026-06-21)

### 修复
- 筛选展开按钮样式修复
- 自动检测溢出

## v0.7.2 (2026-06-21)

### 新功能
- UI 优化：筛选折叠、标题两行、状态标签行、动漫去中字

## v0.7.1 (2026-06-21)

### 新功能
- 视频页筛选加全部
- 设置页功能拆分
- 曾用名
- 输入框优化

## v0.7.0 (2026-06-21)

### 新功能
- 视频页动漫/影视双标签
- 分集海报fallback
- 中字识别修复
